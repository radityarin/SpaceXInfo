<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.radityarin.spacexinfo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@drawable/missionpatchplaceholder"
        android:label="@string/app_name"
        android:roundIcon="@drawable/missionpatchplaceholder"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".widget.NextLaunchWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_launch_widget_info" />
        </receiver>

        <activity android:name=".ui.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.detail.DetailActivity"
            android:theme="@style/AppThemeWithActionBar"
            android:parentActivityName=".ui.home.HomeActivity"/>
    </application>

</manifest>